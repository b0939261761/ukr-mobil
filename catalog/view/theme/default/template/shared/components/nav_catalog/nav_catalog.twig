<nav class="nav-catalog">
  {% if categories %}
    <ul class="nav-catalog__list nav-catalog__list--main">
      {% for child1 in categories %}
        <li class="nav-catalog__item nav-catalog__item--main {{ child1.children ? 'nav-catalog__item--has-children' : '' }}">
          <a class="nav-catalog__link nav-catalog__link--main" href="{{ child1.link }}">
            <svg class="nav-catalog__link-img">
              <use xlink:href="/resourse/images/shared-sprite-icons.svg#icon-{{ child1.icon }}"></use>
            </svg>
            {{ child1.name }}
          </a>

          {% if child1.children %}
            <button class="nav-catalog__btn-nav">
              <svg class="nav-catalog__btn-nav-img">
                <use xlink:href="/resourse/images/shared-sprite-icons.svg#icon-arrow-right"></use>
              </svg>
            </button>

            <ul class="nav-catalog__list nav-catalog__list--child">
            {% for child2 in child1.children %}
                <li class="nav-catalog__item nav-catalog__item--child {{ child2.children ? 'nav-catalog__item--has-children' : '' }}">
                  <a class="nav-catalog__link" href="{{ child2.link }}">{{ child2.name }}</a>

                  {% if child2.children %}
                    <button class="nav-catalog__btn-nav">
                      <svg class="nav-catalog__btn-nav-img">
                        <use xlink:href="/resourse/images/shared-sprite-icons.svg#icon-arrow-right"></use>
                      </svg>
                    </button>

                    <ul class="nav-catalog__list nav-catalog__list--child nav-catalog__list--child-2">
                      {% for child3 in child2.children %}
                        <li class="nav-catalog__item nav-catalog__item--child">
                          <a class="nav-catalog__link" href="{{ child3.link }}">{{ child3.name }}</a>
                        </li>
                      {% endfor %}
                    </ul>
                  {% endif %}
                </li>
            {% endfor %}
            </ul>
          {% endif %}
        </li>
      {% endfor %}
    </ul>
  {% endif %}
</nav>
